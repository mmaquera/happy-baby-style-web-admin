{
  "name": "happy-baby-style-admin",
  "version": "1.0.0",
  "description": "Admin panel for Happy Baby Style database management",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node scripts/dev-full.js",
    "dev:backend": "node scripts/dev-server.js",
    "dev:frontend": "node scripts/dev-frontend.js",
    "cleanup": "node scripts/cleanup-ports.js",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["admin", "database", "supabase", "react", "clean-architecture"],
  "author": "Happy Baby Style",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}